# Legacy Note

This bridge setup has been retired.

Use the current deployment guide instead: DEPLOY_MOBILE_SYNC.md.

Current app mode:

- Local organization inside the app.
- Encrypted sync through the Worker sync endpoint.
- Full-record corpus view, copy, Markdown export, and JSON backup.
- No separate model service setup is required for daily use.